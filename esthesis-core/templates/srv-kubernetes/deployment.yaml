{{ template "deployment.api.template" (merge (dict
  "podName" "esthesis-core-srv-kubernetes"
  "podMaxCpu" "1"
  "podMaxMemory" "256M"
  "podJwtVerifier" true
  "podMongoDb" true
  "podLogging" true
  "podProbes" true
) .) }}
