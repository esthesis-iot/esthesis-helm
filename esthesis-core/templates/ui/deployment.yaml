{{ if not .Values.devMode }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: esthesis-core-ui-deployment
  annotations:
    configmap.reloader.stakater.com/reload: "esthesis-core-ui-cm"
spec:
  selector:
    matchLabels:
      app: esthesis-core-ui
  template:
    metadata:
      labels:
        app: esthesis-core-ui
        kostas: takis
    spec:
      containers:
        - name: esthesis-core-ui
          {{- if .Values.esthesisRegistry }}
          image: {{ .Values.esthesisRegistry }}/esthesisiot/esthesis-core-ui: 3.0.0
          {{- else }}
          image: esthesisiot/esthesis-core-ui: 3.0.0
          {{- end }}
          ports:
            - containerPort: 8080
          imagePullPolicy: Always
{{ end }}
